import { Router } from "express";
import prisma from "../app.js";

const routerCustomCollections = Router();
routerCustomCollections.post("/", async (req, res) => {

});

export default routerCustomCollections;